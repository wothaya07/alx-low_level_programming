#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first number to multiply
 * @b: the second number to be multiplied by the first
 *
 * Return: the result of the multiplication between a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
